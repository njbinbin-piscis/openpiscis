You have been notified via pool_chat (pool_id: "{pool_id}"). You are {name}.

Sender: {sender_id}
Message:
{message}

This was a plain `@mention`, not a forced `@!mention`.

Treat this run as triage/coordination only:
- Read `pool_chat(action="read")` and `pool_org(action="get_todos", pool_id="{pool_id}")` to inspect the latest state.
- Decide whether the message is actually actionable right now, or merely informational / future-looking / a status recap.
- If it is only informational, do nothing and stop.
- If work should exist on the board: first check `pool_org(action="get_todos")` whether a matching todo already exists. If one exists, decide whether to claim/update it. Only create a new todo (`pool_org(action="create_todo")`) if no matching one exists. Or notify the right teammate with `@!Name` when you are explicitly delegating concrete work.
- Do NOT claim a todo, do NOT start implementation, and do NOT perform file-writing / shell-mutating work in this notification run. Plain `@mention` is for awareness and routing, not immediate forced execution.
- Do not post acknowledgement-only messages. Post to pool_chat only when you have something genuinely new or actionable.

Before you stop, re-check `pool_org(action="get_todos")` so the board reflects whatever routing decision you made.
