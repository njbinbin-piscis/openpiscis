# Rust / Cargo
target/
# Note: Cargo.lock is intentionally committed for applications (not libraries)
# Workspace members should not carry their own lockfile; only the workspace root's lockfile is tracked.
src-tauri/piscis-core/Cargo.lock

# Node / npm
node_modules/
dist/
.vite/

# Tauri build outputs
src-tauri/target/
src-tauri/WixTools/
src-tauri/gen/
src-tauri/binaries/
src-tauri/robotz-mcp-*

# Runtime data
*.db
*.db-shm
*.db-wal
.piscis/
config.json

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/settings.json
.idea/

# Logs
*.log
# Ad-hoc CI / build dumps saved at the repo root during local debugging.
/ci-log.txt
/tauri-bundle-log.txt
/*-log.txt
/harness-smoke*.jsonl

# Windows installer artifacts
*.msi

# Local release / test helpers
.tmp-gh/
src-tauri/target-test/
src-tauri/target-test-archive/
examples/string_utils.py
test_output.txt

# Extra local build directories (fast release / tauri fast)
target-fast-release/
target-release-*/
target-tauri-fast/

# Python bytecode caches (bench & harness scripts)
__pycache__/
*.pyc
*.pyo

# Ad-hoc `.log.err` debug dumps (complement existing *.log)
*.log.err

# Partial/intermediate benchmark outputs
scripts/bench_compression/results/*.partial.json

# SWE-lite bench run outputs (nested .git sandboxes, possible secrets in configs)
scripts/bench_swe_lite/results/

# Tribon M3 local reference docs (machine-specific, not for version control)
docs/tribon/

# Local skills development (machine-specific, deployed via Junction to AppData)
/skills/

# Local reference repos / docs
references/


ci-log.txt
src-tauri/ci-log.txt
tauri-bundle-log.txt
.qoder/
